<!DOCTYPE html>
<html>
  <head>
    <title>RailsWatchList</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <div class='p-3 d-flex justify-content-between bg-light'>
      <%= link_to 'All categories', root_path, class: 'text-decoration-none text-secondary' %>
      <%= link_to 'Create a new category', new_list_path, class: 'text-decoration-none text-secondary' %>
    </div>
    <%= yield %>
  </body>
</html>
